namespace Swag.Plugin.Swagger.Constants;

internal static class PatchConstant
{
    internal static string Summary = "Used for making partial updates to an existing resource. PATCH requests are not necessarily idempotent, as they can change the resource's state with each request.";
    internal static string Description = "The PATCH method is used to apply partial modifications to a resource.";
}
